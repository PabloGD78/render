import 'package:flutter/material.dart';

class AppColors {
  // Colores base
  static const Color primary = Color(0xFF2F544D); // Verde oscuro
  static const Color accent = Color(0xFFE9C589); // Crema / beige
  static const Color background = Color(0xFFF7F7F7); // Fondo ligero

  // Colores específicos de la UI
  static const Color pageBackground = Color(0xFFFBF6EF); // Fondo de página (Nuevo)
  static const Color cardBackground = Color(0xFFFEF7E9); // Fondo de tarjeta (Nuevo)
  static const Color textFieldBackground = Color(0xFFFBEBCF); // Fondo de campo de texto (Nuevo)
  static const Color iconColor = Color(0xFFB99A70); // Color de ícono (Nuevo)
  static const Color hintTextColor = Color(0xFFB99A70); // Color de texto de sugerencia (Nuevo)

  static const Color primaryLight = Color(0xFFFAEBCF); // Beige más claro
  static const Color lightTextColor = Color(0xFFFFFFFF); // Blanco

  // ⚡ Añadido color de texto general
  static const Color text = Color(0xFF333333); // Texto general oscuro

  // Espaciados
  static const double kPadding = 24.0;
  static const double kMargin = 20.0;
}
